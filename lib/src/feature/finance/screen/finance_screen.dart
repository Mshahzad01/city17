import 'package:flutter/material.dart';

class FinanceScren extends StatelessWidget {
  const FinanceScren({super.key});

  @override
  Widget build(BuildContext context) {
    // ignore: prefer_single_quotes
    return const Scaffold(body: Center(child: Text("Finance Screen")));
  }
}
